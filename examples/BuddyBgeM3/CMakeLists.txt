# This example contains no CMake build targets: all tools are standalone
# shell scripts for exporting artifacts on x86 and for building/benchmarking
# BGE-M3 RAX natively on SpacemiT K3 (see README.md).
