# -*- Python -*-
#
# This directory includes lit tests (test_batch_*.py, etc.) and offline tooling.
# The tooling scripts should be excluded from lit to avoid UNRESOLVED/invalid RUN handling.
#

for name in [
    "aten_op_batch_runner.py",
    "aten_op_numeric_worker.py",
    "run_numeric.py",
    "generate_op_catalog.py",
    "generate_op_tests.py",
]:
    config.excludes.add(name)
