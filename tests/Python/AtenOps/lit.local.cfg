# -*- Python -*-
#
# This directory contains python lit tests plus helper tooling. Exclude helper
# scripts from lit discovery.
#

for name in [
    "run_numeric.py",
]:
    config.excludes.add(name)
