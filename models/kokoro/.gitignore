# Kokoro-generated artifacts and staged HF snapshot files.
# `buddy_add_model` re-imports / re-stages these from the local HF snapshot
# (BUDDY_KOKORO_MODEL_PATH) at build time, so nothing here is committed.

# Import output (incl. stale artifacts from the original PR)
arg0.data
layer_partitioned/

# Python caches
__pycache__/

# HuggingFace snapshot files (staged via ASSET_FILES / KOKORO_MODEL_PATH)
config.json
kokoro-v1_0.pth
voices/
samples/
eval/
DONATE.md
EVAL.md
README.md
SAMPLES.md
VOICES.md
.gitattributes
artifact.metadata
model.safetensors
pytorch_model.bin

# Root-level compiled IR (build/ contains the real generated outputs)
forward.mlir
subgraph0.mlir
